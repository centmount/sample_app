<%= form_for(current_user.reactions.find_by(micropost_id: micropost.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= button_tag( class: "btn btn-default") do %>
    <%= content_tag :span, micropost.react_users.count, class: "glyphicon glyphicon-star" %>
  <% end %>
<% end %>

